# Cohesive implementation phase — approved scope unchanged

Continue the same session and all requirements in the complete approved brief. Codex has inspected the foundation/build/test entrypoints and enabled the commands listed in this job under a greater approval revision; this records execution of already user-approved implementation/testing, not permission for external actions. Claude retains implementation and test ownership; Codex retains independent review, git, publication and installation. No authenticated model smoke tests yet. No project/customer data, credentials, global installations/configuration changes, git writes or publishing.

Complete the actual product end to end: versioned contract and SDK adapter, durable task-isolated workers/broker and trust, interactive permissions/questions/messages, event transport/supervision/usage, real MCP tools, functional React dashboard, legacy reliability regressions, documentation, prepared reproducible runtime bundle. Write and run behavioral tests before implementation; then implement until they pass. Foundation tests are not the final acceptance suite. Do not substitute source-text assertions, fake production adapters, mocked broker/HTTP/worker behavior, static UI, or TODOs for working integration. Keep modules focused and independently testable without reducing the feature scope. You can complete all cohesive in-scope development now; do not stop at each module for another permission checkpoint. Report only concrete blockers or a meaningful completed review checkpoint.

Codex independently ran the first npm test checkpoint: actions, contract, events and state-files all fail loading their not-yet-created production modules. This confirms the command executes and no assertions have passed, but it is not yet behavioral RED. Add the minimal API surface as needed, observe the relevant behavior assertions fail, then implement the behavior and rerun. Do not report missing-import failures as proof the assertions detect regressions.

Codex reproduced your npm installation failure independently. `npm --prefix outputs/claude-code-live/runtime install --ignore-scripts --no-audit --no-fund --dry-run` incorrectly looks for the checkout-root package.json in this environment. The same `npm install --ignore-scripts --no-audit --no-fund --dry-run` with the actual cwd set to runtime resolves all 208 packages successfully. No dependencies were installed by that simulation. The job now also authorizes the explicit PowerShell WorkingDirectory install command; use it exactly as listed in allowedCommands. `npm --prefix ... test` does execute in the correct script directory, so the existing test/build commands can remain. Do not create an unrelated root package.json to work around this environment issue.

Additional independent review findings:

- The status write failure is proven: run04 File.Move access denied at line40. Reader contention is a leading hypothesis, not proven identity of the offending reader. Use deletion-sharing on owned readers and short bounded retries, report observability degradation without killing healthy execution, and explicit terminal persistence fallback. Capture sanitized inner exception/HRESULT in regression diagnostics where useful. Do not silently swallow terminal result failures. In v2 keep transport activity, visible progress, persistence health and result state distinct.
- The legacy preparation timing test has only about 100ms margin on this machine because its 1-second cap includes cold PowerShell startup. Make the fixture robust with preparation intentionally longer than a realistic runtime cap and assert both preparation/total and measured runtime; retain the semantic guarantee that preparation does not consume the runtime budget.
- A prepared runtime must actually exist in the published/installable plugin. Current runtime/.gitignore ignores dist/. Ensure compiled production assets are included in the release source/bundle by a maintainable tracked location or explicit reproducible packaging step; never rely on local node_modules or network install at session start. Preserve third-party licenses/notices in distributed bundles. Do not silently bundle/use another Claude CLI.
- Check actual dependency package exports before using require(packageName + '/package.json') in build metadata: some packages restrict this export. Check SDK runtime dynamic assets/native dependencies with a smoke test from an isolated built-only installation, not just the source tree.
- Test harness cleanup review: helpers/pwsh.ts spawnHolder rejects on readiness timeout without killing/awaiting its holder process; helpers/broker-client.ts can similarly throw before returning a cleanup handle. Put failed-start cleanup inside the helpers themselves, release only test-owned exact processes/resources, and retain diagnostics. Test failures must not leave a mutex/file holder or broker behind.
- In broker-security.test.ts the manual bootstrap assigns broker.cookie, but helpers/broker-client.ts browserHeaders/browserActionHeaders close over the original local cookie string (empty with bootstrap:false). Make the helper read its current cookie property or provide an explicit session setter, so subsequent authenticated tests exercise the intended identity.
- Critical foundation-test bug: state-files.test.ts initializes pwsh=false, then evaluates every `{skip: !pwsh && ...}` at test registration BEFORE its before() hook sets pwsh. Therefore all Windows sharing/telemetry/fallback regressions are always skipped. Use top-level awaited capability discovery before registration or call test context.skip() after checking inside each test. On this Windows host those regressions must execute, not pass as skipped.
- Independent reviewer confirmed these findings and added: startTestBroker.stop() installs its fallback only AFTER awaiting shutdown HTTP; start a deadline first and abort a hung response. The atomic concurrency test accepts undefined even though readJsonShared returns undefined for malformed/torn JSON, so a non-atomic writer can pass; once the file exists require complete valid snapshots including payload. Settle/propagate writer rejection and bound the reader loop (stop currently only becomes true on successful writer completion). Add classifier regressions for chained commands, command substitution, redirection, git -C/npm --prefix outside the workspace and scope-prefix collisions. Treat text classification as guardrails, not a sandbox.
- Approval of an MCP connector does not prove that a tool named search is read-only. Do not classify arbitrary MCP actions by name alone as safe. Require approved tool capability/annotation plus policy, or escalate unknown tool semantics. Likewise development shell commands can execute project scripts; a `npm test` prefix is not proof of scope or safety. Trusted-resource policy and clear limitations must match actual behavior.
- Inventory tests must not make production ignore user/local/ancestor resources just to satisfy a fixture listing project files. Inject explicit fixture discovery roots into test inventory where needed, include user settings/local settings/agents/skills/hooks/MCP and their invalidation, and never read/approve real user configuration for a fake test. Symlink/reparse containment must be checked with actual filesystem fixtures, not only lexical paths.
- Apply the skip-at-registration correction to legacy-compat.test.ts too. Its retry test releases the holder after 700ms BEFORE launching a fresh PowerShell, which can take longer than that; use a real ready/attempt barrier. Bound its marker wait loop and propagate failed reader startup. Quota serialization should await actual first acquisition, not guess that a 100ms sleep is enough. If pwsh is unavailable, use explicit test skip/failure rather than returning as a successful assertion-free test.
- Reviewer confirmed broker-tasks.test.ts cancellation currently checks worker A/B and broker but never spawns a real descendant. Add an isolated fake-tool child (and preferably grandchild), verify its actual liveness before cancellation, then verify only that task's descendants end while the other task continues. A worker-only kill must fail this regression. Also remove assertion-free PASS paths when pwsh is absent (including broker-tasks).
- Broker lifecycle regression review: message delivered is not response generated (broker-tasks229-240); wait separately for response/turn completion. Coordinator-presence test waits longer than expiry after a browser read, so both correct and incorrect refreshed presence become absent; assert timestamp unchanged or probe inside the distinguishing interval. Bound broker-security SSE first-chunk wait and abort in finally; cleanup failed MCP connections too. Doctor's own authenticated:null claim is not proof that no auth command ran; use a fake launcher invocation trace.
- Mandatory fake-test boundary: missing or broken fake adapters MUST fail closed, never invoke the real SDK/Claude as fallback. Inspect the complete harness before executing scenarios, isolate fixture state/auth configuration and remove inherited API/provider credentials from test children. curl/git push scenario strings are data for the simulator, not executable shell commands. Add a fake-launcher trace regression proving that harness failures do not start real Claude. No authenticated generation is authorized in the implementation suite.
- Build artifact test's blanket ban on every https string in emitted JS will flag inert React diagnostic/documentation URLs and other non-network strings. Test actual asset references and browser network requests (no CDN, telemetry, external fonts/resources) instead of requiring harmless dependency strings to disappear.
- Legacy exact Bash rules prohibit a colon character. Use the enabled aggregate `npm ... run verify` for browser tests if the test:browser script cannot be separately delegated. No shell chaining or altered commands in this bootstrap; Read/Glob/Grep/Write/Edit remain available. This limitation belongs to the bootstrap runner, not the new development permission design.
- Task identity cannot rely on an arbitrary MCP argument or a shared MCP server's initial environment. Provide the terminal bootstrap that derives the real current task environment and issues a task-scoped capability. MCP routes are bound to that capability; browser user controls are separately authenticated and audited. Document that local same-user processes are outside strong isolation guarantees.
- Use full native Claude coding system prompt explicitly; SDK default is minimal. Keep xhigh configured and effective model observed separately. Don't imply server-confirmed effort where there is only sent configuration. Unknown quota must not block a user-authorized fixed model. setModel only between turns, never mid-tool.
- Important preflight correction: preflight.test.ts invents sdkBundledCliVersion=2.1.263 and assumes every installed CLI older than the SDK bundle is incompatible. The actual SDK bundle version is expected 2.1.270 and our installed CLI is 2.1.263; package pairing alone is not evidence of a minimum supported CLI. Inspect installed SDK and probe required protocol capabilities without authenticated generation; block actual unsupported features, not a guessed strict version equality/order rule. Do not claim compatibility based on invented versions. supportedModels() returns actual descriptor shapes and may list aliases rather than every accepted full model ID; inspect declarations and use actual init/provider evidence before declaring a full authorized ID unsupported. Preserve unknown vs confirmed distinctions.
- Codex checked the existing CLI's readonly authentication status: loggedIn=true, authMethod=claude.ai, apiProvider=firstParty, subscriptionType=max (no account identity retained). `claude auth status --json` is supported. New runtime preflight must refuse or explicitly require separate authorization for API-key/cloud-provider authentication paths rather than silently consuming billable API credentials inherited from environment. Never expose email, org, credentials or raw auth payloads in logs/UI.

Consult the coordinator's review-notes.md for the approved visual reference and SDK configuration discoveries. Apply the reference as real React UI, with actual state and working controls, no invented quota/progress or inert decorative controls. Preserve authenticated loopback and render all messages as untrusted escaped text.

## Buzz reference requested by the user during implementation

Codex read the upstream README, ARCHITECTURE.md, crates/buzz-acp/README.md and TESTING.md at https://github.com/block/buzz. Use architectural ideas that fit our already approved scope; do not add Buzz, Nostr, Docker, Redis, Postgres or ACP as new mandatory dependencies or alter authentication. Our SDK adapter and MCP remain the implementation.

Useful parallels: central authoritative event state shared by human UI and agent clients; task-scoped identities and ordered message queues; visible authorship and decisions; independent UI disconnect vs worker lifetime; bounded event delivery with cursor recovery. In particular, close the history-to-live subscription race: subscribe before/overlapping replay and deduplicate by sequence, so events created during history fetch are not lost. Test that exact boundary with concurrent append/reconnect. Make the dashboard a task collaboration room with public messages, tools, available results and pending decisions, not a stream of tool names.

Deliberate differences: no timer-based turn cancellation, no silent API-key billing, no automatic replay of uncertain side effects, no unapproved additional models. Buzz's documented lifecycle/test ideas are references, not verified guarantees to repeat in our documentation. If any upstream code is actually reused later, preserve its applicable license/attribution; this phase requests pattern inspiration, not copying or integrating the Buzz service.

Report changed areas, exact tests and observed results, unresolved limitations, and installed-CLI compatibility findings at the completed checkpoint. A passing fake suite is not permission to claim real-model validation or publish.
