# OpenAInthropic — approved implementation brief

You are Claude, the implementation and test owner for this approved project. Codex owns planning, initial inspection, independent review, commit, push and release/install. Do not commit, push, merge, deploy, change installed plugins/configuration, access customer data, credentials, other projects, or launch authenticated model sessions. Work only in C:/www/claude-code-live-plugin/work/openainthropic-v2 on branch codex/openainthropic-interactive. Existing production CLI sessions elsewhere must survive. Read the repository as needed for implementation. Public explanations, artifacts and reproducible tests are our collaboration interface; do not output hidden reasoning. Use Write/Edit for source edits. This first dispatch uses the installed legacy runner, not the new code you will build; never edit the installed runner to change your own permissions.

The user approved the following complete objective. Build it cohesively; question concrete design flaws and report blockers rather than silently deleting requirements. Codex will inspect and answer your report, then resume this same session.

## Product contract

OpenAInthropic is an independent local integration, not an official OpenAI/Anthropic partnership. Preserve technical identifier, skill name and marketplace claude-code-live / haise-local. Visible tagline Codex com Opus e Fable. Default to a web dashboard; keep the old terminal as explicit fallback.

Only claude-fable-5-1 and claude-opus-5 are authorized working models, always effort xhigh (Extra). Codex chooses model with a reason based on task/capacity. Record requested and actually observed model distinctly, effort as configured unless server confirmation exists. No silent effort/model downgrades. No plugin-imposed turn/token/runtime caps in new v2 jobs; provider constraints still apply. Never activate API billing or extra credits automatically.

Develop using Node 22+ and TypeScript, official @anthropic-ai/claude-agent-sdk (npm currently 0.3.270; read actual installed declarations, do not invent SDK APIs), @modelcontextprotocol/sdk (1.30.0). Persistent streaming input with canUseTool and PreToolUse as needed. Preserve the installed CLI and its normal subscription auth. Official support https://support.claude.com/en/articles/15036540-use-the-claude-agent-sdk-with-your-claude-plan says SDK and claude -p subscription changes are paused; no assumption of new API billing. CLI version is currently 2.1.263. Detect genuine incompatibility and report it, never invoke a different model as fallback. SDK can use existing CLI through its supported executable-path option; ensure Windows npm-shim handling. Avoid bundling a different CLI unnoticed.

New runtime contract is versioned, keeps coordination fields and all eight owners. Execution requires approved nonempty plan, revision, scope and trusted workspace; Claude only edits/tests when assigned. Development profile permits full native dev tools, commands, testing and investigation within approved scope; remove mandatory safe-mode, dontAsk and blanket prompt denial in this profile. Keep read/restricted legacy modes without broadening old jobs. Development commands should not need a static exact list per command; classify/escalate actions not known to be in scope. Protect reserved operations via rules and PreToolUse (canUseTool alone doesn't see auto-approved tools). Do not claim text filters are an OS sandbox. No secrets/PII in jobs/logs/frontend, no arbitrary traversal/file readers. Block sensitive files and risky external actions, with intelligible reasons.

Inventory project/user customizations BEFORE executing them: CLAUDE.md, instructions, skills, agents, hooks, MCPs and relevant settings. Trust is approved once per canonical project and content fingerprint, stored outside checkout with identity/revision; material changes invalidate trust. Approving an MCP server does not authorize its external mutations. Unknown resources remain disabled/pending approval. Avoid loading unapproved user/project settings automatically. Preserve actual intended builtin Claude coding system prompt rather than replacing it with an impoverished custom prompt. Working subagents must not silently use other model/effort tiers; respect native internal mechanisms and report limits honestly.

One durable Claude session identity per genuine Codex task, a separate process worker per active task. Broker local HTTP service and MCP use the same state/actions. Expose start, status/list, event wait with cursor, message, answer, interrupt, end, model selection between turns, inventory/trust, dashboard URL. Derive Codex task from actual environment/session association, don't accept arbitrary cross-thread impersonation. Browser user actions have their own source identity and recorded audit. No infinite auto-retry/autoresume of uncertain mutation. Same-task runs serialize; workspace writer lock across tasks prevents concurrent editing of same checkout (reads can coexist, separate worktrees can edit in parallel). Locks must outlive browser/MCP client sessions and reconcile process identity on crash. Closing browser doesn't stop workers. Termination scoped to exact process and descendants, never all node/claude processes.

Message queue: when busy, subsequent guidance is queued for next turn, persisted with id/source/received/delivered state; only explicit interrupt aborts a turn. Unique permission/question requests include task, run, request ID and lifecycle; reject duplicate/stale/wrong-run answers. Resolve SDK callbacks visibly. CanUseTool can await indefinitely, waiting for permission is not failure or inactivity. On service restart detect uncertain in-flight work and require review; never blindly replay queued messages/mutations. Model changes happen only between turns; reuse context where supported, otherwise linked successor with handoff decisions/remaining work and explicit reason.

Supervision: 20 minutes without activity and 2 hours elapsed are alerts, not kill timers. Distinguish busy tools, waiting permission, disconnected, uncertain, coordinator absent, idle and terminal states. No fake always-running Codex: heartbeat/event-wait from actual coordinator updates presence, otherwise show aguardando coordenador. No recurring automation creation. Codex remains responsible for independent artifact acceptance; CLI COMPLETED is transport success only.

Quota: reuse existing sanitized /usage parser and GLOBAL mutex so v1/v2 queries don't overlap. Consult before start and between turns; timestamp attempts and observations. Unknown remains unknown. 3% is a recommendation to consider the other allowed model, not a hard plugin block or mid-run kill. Shared limits remain shared; no workaround by opening more sessions. Failures must be visible and not prevent fixed authorized model work solely because telemetry failed. Don't invent monetary amounts or continuous telemetry.

Events: durable append-only sequenced records with timestamps, thread/run/tool correlation; derive status.json, resultado.json and acompanhamento.txt for compatibility. Handle public text deltas without double-counting completed messages. Preserve tool-start/input metadata and eventual available tool results, errors and stderr safely. Never persist thinking/signatures/raw secrets. Large outputs should have bounded safe previews and paging, not unbounded memory growth, with explicit truncation; don't arbitrarily limit Claude's own output. Crash-safe state/pointers, UTF8, incomplete last log line, reconnect replay without duplicate/lost events. Prepared failure must end visibly and preserve last usable session pointer.

Dashboard: Portuguese dark graphite/navy, violet brand accent. React + Vite static bundled frontend, no CDN/network resources. Task list active/history sidebar; main chronological public conversation/tool feed with expandable safe inputs/results/errors and times; inspector model requested/confirmed, Extra configured, current tool, last activity, elapsed, quota timestamp/unknown, changed files and independent review pending. Native accessible controls to queue orientation, answer question/permission, interrupt turn and end session. Closing tab doesn't stop session. Distinguish observed workspace changes from proven Claude-authored changes; never attribute all git diff to Claude. Actual errors replace generic failure. No fake progress percentages or hidden reasoning. Do not claim live terminal bytes if SDK only returns tool result on completion. Codex will provide a generated visual reference before you implement UI.

HTTP security: loopback only, random per-user secret/bootstrap with authenticated HttpOnly SameSite session cookie, validate Host/Origin and CSRF for actions, no wildcard CORS, don't expose token in logs/referers, serve only fixed static assets and explicit authenticated APIs. SSE authorization, cursor replay and backpressure. All content untrusted: React escaping, no unsafe HTML/JS links, CSP, sensitive-field filtering; redaction is best effort, not infallible. Auth required for read APIs too. MCP stdio adapters attach to shared broker without duplicating workers. New dependencies pinned in lockfile; bundle compiled runtime/production dependencies reproducibly, no npm install on session start. Don't commit node_modules/credentials/generated runtime logs.

## Tests and rollout

Use test-driven implementation. First write behavior tests that fail for the missing feature, then minimal production code. Fake only external SDK/model boundary, exercise real worker/broker/HTTP/MCP processes and persistence. Test xhigh/exact model/preflight, approvals/trust invalidation, message queue/multiturn/interrupt/stale answers, concurrent tasks, writer lock, quota serialized with v1, alerts never killing, preparation failure, crash/uncertain recovery, UI safe text/UTF8/reconnect/large log, legacy contract preservation. Built artifacts must be executable in isolated installation. New frontend must have browser functional tests, not just source string assertions.

Baseline current main 39c24cd is clean. Manifest/skill/contract validation passed. The baseline runtime suite failed at claude-runtime.tests.ps1:209 'Preparation time must not consume the Claude runtime limit': fake test gives total process only 1 second and delays preparation 1500ms. Investigate actual startup/timeout cause, add diagnostics and fix real defect or robust test synchronization; do not simply delete assertion or weaken semantics. Existing tests clean up their temp runs. No implementation diff caused this failure.

Codex will run independent review and regression tests. Small real disposable per-model tests are authorized later but reserved until Codex inspects the harness and explicitly dispatches that validation. No user project/customer data. No billable API key setup. Publish/install only Codex after all validations. Keep old running versions intact. New thread needed for new MCP tools. Preserve base manifest version; Codex cachebuster helper later updates suffix. Update READMEs and skill docs to describe reality. Keep meaningful scoped results, not bare success claims.

## First checkpoint — dependency contract and failing foundation tests

For this first dispatch, create the runtime/frontend package manifests, TypeScript/test/build configurations and failing foundation behavioral tests with literal expected values and fake external adapter shape. Do NOT implement production backend/UI yet: Codex must inspect test/build entrypoints before enabling their execution in the legacy bootstrap runner. Put runtime at outputs/claude-code-live/runtime and use npm scripts with npm --prefix outputs/claude-code-live/runtime ... commands. Pin exact dependency versions after npm view if needed; npm install --ignore-scripts is authorized in this runtime path only. A package manager lockfile is allowed. No npm lifecycle scripts are allowed during dependency install. Prefer esbuild for reproducible runtime bundle and node:test for backend tests; use React/Vite for frontend. Minimize dependencies sensibly but fulfill interactive SDK/MCP, do not replace them with fake implementations.

Read installed SDK types after install to validate the design. At checkpoint report exact commands Codex should approve, files created, dependency/API compatibility discoveries, failing expected tests (if you cannot yet run test commands, say explicitly not run), and any concrete blockers. Do not claim all features done. Subsequent dispatch gives you the full cohesive implementation phase with test commands enabled, not isolated microtasks.
